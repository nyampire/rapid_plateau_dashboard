# OSM extracts / intermediate geometry (large, regenerated by the batch)
*.osm.pbf
*.geojsonseq
work_*/
poc_mem/

# Source Excel (large; lives in plateau_attributedata)
*.xlsx

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local env / secrets — never commit
.env
*.local
*.local.md
.DS_Store

# Local Claude Code dev config (preview launch settings)
.claude/
